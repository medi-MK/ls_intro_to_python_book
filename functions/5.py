# It doesn't print anything as the `print` function isn't used. 
# The function `scream` returns the value 'Yipeee!!!!' but the
# value isn't stored anywhere

def scream(words):
    return words + '!!!!'

scream('Yipeee')